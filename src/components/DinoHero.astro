---
// DinoHero.astro
---

<div class='hero-container'>
  <div class='hero-image'></div>
  <div class='hero-text'>
    <h1>Dino Friends Safari</h1>
    <a href='#' class='cta-button'>Start Your Adventure!</a>
  </div>
  <div class='dino' role='img' aria-label='Dinosaur'>🦕</div>
</div>

<style>
  .hero-container {
    position: relative;
    width: 100%;
    height: 400px;
    overflow: hidden;
    border-radius: 20px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }
  .hero-image {
    width: 100%;
    height: 100%;
    background-image: url('/images/dino-hero.webp');
    background-size: cover;
    background-position: center;
    transition: transform 0.3s ease;
  }
  .hero-container:hover .hero-image {
    transform: scale(1.05);
  }
  .hero-text {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    color: #fff;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
  }
  h1 {
    font-size: 3em;
    margin-bottom: 0.5em;
  }
  .cta-button {
    display: inline-block;
    padding: 10px 20px;
    background-color: #ffd100;
    color: #333;
    text-decoration: none;
    border-radius: 25px;
    font-weight: bold;
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease;
  }
  .cta-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
  }
  .dino {
    position: absolute;
    bottom: 20px;
    left: 20px;
    font-size: 4em;
    transition: transform 0.3s ease;
  }
  .hero-container:hover .dino {
    transform: translateY(-20px) rotate(10deg);
  }
</style>
